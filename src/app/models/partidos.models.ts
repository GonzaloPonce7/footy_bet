export interface Partido {
    id: string;
    local: string;
    visitor: string;
    competition_name: string;
    date: string; 
    local_shield: string;
    visitor_shield: string;
    result: string; 
}